# Postgres + Pgadmin deployment

## Env
Put env vars to `.env`.

Example in `.env.example`.

### Make commands works only in Linux

## Run
```bash
make run
```

## Stop
```bash
make stop
```

## Stop and Remove containers
```bash
make down
```

## Clean
to clean volumes (requires sudo)
```bash
make clean
```